input {
	tcp {
		port => 5000
	}
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => ["elasticsearch:9200"]
    index => "app-%{+YYYY.MM.dd}"
    user => "elastic"
    password => "DFLvl9xmEaeOxy9eujfC"
	}
}
